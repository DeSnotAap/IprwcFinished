export const environment = {
  production: true,
  apiUrl: 'https://nurturing-consideration-production.up.railway.app/api/' // This would be changed to the production API URL in a real deployment
}; 
